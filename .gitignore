__pycache__/
*.pyc
*.pyo
.DS_Store
*.egg-info/
dist/
build/
node_modules/
.env
evolve-test/
evolve-workspace
*-workspace/

# Platform mirrors — generated by scripts/sync-*.sh from plugin/ at
# install time. Do not commit; first-time Codex/OpenCode users should run
# `bash scripts/sync-codex.sh` or `bash scripts/sync-opencode.sh` locally.
.agents/
.opencode/

# Private docs — local only, not published to GitHub.
# Lives in docs/private/, contains: presentation母本、公众号文章草稿、
# self-iteration reports、design comparison notes、以及其它只给作者本人
# 或线下汇报/路演/文章用的材料。Public docs stay in docs/ at the top level.
docs/private/
plugin/skills/skill-evolver.zip
